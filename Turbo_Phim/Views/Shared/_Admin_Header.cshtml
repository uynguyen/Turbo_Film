@using Microsoft.AspNet.Identity

@{
    Layout = null;
}

<div class="navbar navbar-default" role="navigation">

        <div class="navbar-collapse collapse">
            <ul id="main-menu" class="nav navbar-nav">
                <li>
                    <a class="" href='@Url.Action("Index", "AdminMain")'><span class="navbar-brand"><span class="fa fa-paper-plane"></span> Turbo Phim Admin</span></a>
                </li>
                <li>
                    <a class="" href='@Url.Action("Index", "Home", new { homepage = true})'><span class="navbar-brand"><span class="fa fa-paper-plane"></span>Chuyển sang trang chủ</span></a>
                </li>

            </ul>

        <ul id="main-menu" class="nav navbar-nav navbar-right">
           
                    @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
                    {
                    @Html.AntiForgeryToken()

                    <ul class="nav navbar-nav navbar-right">
                        <li>
                            @Html.ActionLink(User.Identity.GetUserName(), "UpdateProfile", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })

                        </li>
                        <li><a href="javascript:document.getElementById('logoutForm').submit()">Đăng xuất</a></li>
                    </ul>
                    }         
        </ul>
    </div>
</div>